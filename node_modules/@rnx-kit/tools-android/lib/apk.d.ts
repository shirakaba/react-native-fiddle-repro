import type { DeviceInfo, PackageInfo } from "./types.js";
/**
 * Returns the package name and the first launchable activity of the
 * specified APK.
 */
export declare function getPackageName(apk: string): PackageInfo | Error;
/**
 * Installs the specified APK on specified emulator or physical device.
 *
 * @remarks
 * This function automatically uninstalls the existing app if an
 * `INSTALL_FAILED_UPDATE_INCOMPATIBLE` error is encountered.
 */
export declare function install(device: DeviceInfo, apk: string, packageName: string): Promise<Error | null>;
/**
 * Starts the specified activity on specified emulator or physical device.
 * @param options
 * @param packageName
 * @param activityName
 */
export declare function start({ serial }: DeviceInfo, packageName: string, activityName: string): Promise<{
    stdout: string;
    stderr: string;
    status: number | null;
}>;
//# sourceMappingURL=apk.d.ts.map