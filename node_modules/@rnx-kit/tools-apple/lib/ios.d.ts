import type { BuildParams, Device, DeviceType, Logger, Simulator } from "./types.js";
export declare const iosDeploy: (...args: string[]) => Promise<{
    stdout: string;
    stderr: string;
    status: number | null;
}>;
/**
 * Returns a list of available iOS simulators.
 */
export declare function getAvailableSimulators(search?: string): Promise<Record<string, Simulator[]>>;
/**
 * Returns a list of available iOS simulators and physical devices.
 */
export declare function getDevices(): Promise<Device[]>;
/**
 * Boots the simulator with the specified UDID.
 */
export declare function bootSimulator(simulator: Simulator): Promise<Error | null>;
/**
 * Installs the specified app bundle on specified simulator or physical device.
 */
export declare function install(device: Device, app: string): Promise<Error | null>;
/**
 * Adds iOS specific build flags.
 */
export declare function iosSpecificBuildFlags(params: BuildParams, args: string[]): string[];
/**
 * Launches the specified app bundle on specified simulator or physical device.
 */
export declare function launch(device: Device, app: string): Promise<Error | null>;
/**
 * Returns the simulator or physical device with the specified name.
 *
 * @remarks
 * If a simulator is found, it is also booted if necessary
 */
export declare function selectDevice(deviceNameOrPlatformIdentifier: string | undefined, deviceType: DeviceType, logger: Logger): Promise<Device | null>;
//# sourceMappingURL=ios.d.ts.map