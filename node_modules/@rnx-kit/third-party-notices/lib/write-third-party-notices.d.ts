import type { BasicSourceMap } from "metro-source-map";
import type { License, SourceMap, WriteThirdPartyNoticesOptions } from "./types";
/**
 * Function to write third party notices based on the specified source map.
 *
 * This function will read the sourcemap file and tries to find all files
 * that are referenced in the sourcemap by assuming that all dependencies are
 * represented as `node_modules\moduleName` or `node_modules\@scope\moduleName`
 * It will then look in the package.json file to see if it finds a license declaration
 * or it will look for the file called `LICENCE` or `LICENSE` in the root. And aggregate all these
 * files in the output file.
 *
 * This function works for npm, yarn and pnpm package layouts formats.
 *
 * At the moment this function only supports webpack based bundles, there is nothing
 * preventing adding metro support, the current customers of this module are based on
 * webpack at the moment.
 *
 * @param options See IWriteThirdPartyNoticesOptions for more details
 */
export declare function writeThirdPartyNotices(options: WriteThirdPartyNoticesOptions): Promise<void>;
export declare function writeThirdPartyNoticesFromMap(options: WriteThirdPartyNoticesOptions, moduleNameToPathMap: Map<string, string>): Promise<void>;
export declare function getCurrentPackageId(rootPath: string): Promise<string | undefined>;
export declare function normalizePath(p: string, currentPackageId?: string): string;
export declare function splitSourcePath(rootPath: string, p: string): string[];
export declare function parseModule(options: WriteThirdPartyNoticesOptions, moduleNameToPath: Map<string, string>, p: string): void;
export declare function parseSourceMap(options: WriteThirdPartyNoticesOptions, currentPackageId: string | undefined, moduleNameToPath: Map<string, string>, sourceMap: SourceMap): void;
export declare function extractModuleNameToPathMap(options: WriteThirdPartyNoticesOptions, currentPackageId: string | undefined, sourceMap: SourceMap): Map<string, string>;
export declare function extractLicenses(moduleNameToPathMap: Map<string, string>): Promise<License[]>;
export declare function gatherModulesFromSources(sources: BasicSourceMap["sources"], options: WriteThirdPartyNoticesOptions): Map<string, string>;
//# sourceMappingURL=write-third-party-notices.d.ts.map