"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getKitCapabilities = getKitCapabilities;
const console_1 = require("@rnx-kit/console");
const semver_1 = __importDefault(require("semver"));
function getKitCapabilities({ capabilities = [], kitType = "library", reactNativeVersion, reactNativeDevVersion: rawDevVersion, customProfiles, }) {
    if (!reactNativeVersion ||
        (!semver_1.default.valid(reactNativeVersion) &&
            !semver_1.default.validRange(reactNativeVersion))) {
        throw new Error(`'${reactNativeVersion}' is not a valid version range`);
    }
    if (kitType === "app" && rawDevVersion) {
        (0, console_1.warn)("'reactNativeDevVersion' is not used when 'kitType' is 'app'");
    }
    const reactNativeDevVersion = rawDevVersion || semver_1.default.minVersion(reactNativeVersion)?.version;
    if (!reactNativeDevVersion ||
        !semver_1.default.subset(reactNativeDevVersion, reactNativeVersion)) {
        throw new Error(`'${reactNativeDevVersion}' is not a valid dev version because it does not satisfy supported version range '${reactNativeVersion}'`);
    }
    return {
        capabilities,
        kitType,
        reactNativeVersion,
        reactNativeDevVersion,
        customProfiles,
    };
}
//# sourceMappingURL=getKitCapabilities.js.map