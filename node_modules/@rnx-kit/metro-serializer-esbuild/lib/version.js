"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.v = v;
exports.assertVersion = assertVersion;
const metro_1 = require("@rnx-kit/tools-react-native/metro");
function v(version) {
    const [major, minor = 0, patch = 0] = version.split("-")[0].split(".");
    return Number(major) * 1000000 + Number(minor) * 1000 + Number(patch);
}
function assertVersion(requiredVersion) {
    const version = (0, metro_1.getMetroVersion)();
    if (!version) {
        throw new Error(`Metro version >=${requiredVersion} is required`);
    }
    if (v(version) < v(requiredVersion)) {
        throw new Error(`Metro version >=${requiredVersion} is required; got ${version}`);
    }
}
//# sourceMappingURL=version.js.map