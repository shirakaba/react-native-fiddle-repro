"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExternalFileCache = exports.ProjectFileCache = void 0;
const tools_node_1 = require("@rnx-kit/tools-node");
const nodefs = __importStar(require("fs"));
const snapshot_1 = require("./snapshot");
class ProjectFileCache {
    constructor(fileNames, fs = nodefs) {
        this.files = new Map();
        this.filesystem = fs;
        fileNames.forEach((fileName) => this.set(fileName));
    }
    has(fileName) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        return this.files.has(normalized);
    }
    set(fileName, snapshot) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        const file = this.files.get(normalized);
        if (!file) {
            this.files.set(normalized, new snapshot_1.VersionedSnapshot(normalized, snapshot, this.filesystem));
        }
        else {
            file.update(snapshot);
        }
    }
    remove(fileName) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        this.files.delete(normalized);
    }
    removeAll() {
        this.files.clear();
    }
    getFileNames() {
        return Array.from(this.files.keys());
    }
    getVersion(fileName) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        return this.files.get(normalized)?.getVersion();
    }
    getSnapshot(fileName) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        return this.files.get(normalized)?.getSnapshot();
    }
}
exports.ProjectFileCache = ProjectFileCache;
// TODO: the files in this list need to be watched. on change/delete, remove from this list and let the file be re-cached on demand
class ExternalFileCache {
    constructor(fs = nodefs) {
        this.files = new Map();
        this.filesystem = fs;
    }
    getSnapshot(fileName) {
        const normalized = (0, tools_node_1.normalizePath)(fileName);
        let file = this.files.get(normalized);
        if (!file) {
            file = new snapshot_1.VersionedSnapshot(normalized, undefined, this.filesystem);
            this.files.set(normalized, file);
        }
        return file.getSnapshot();
    }
}
exports.ExternalFileCache = ExternalFileCache;
//# sourceMappingURL=cache.js.map