import ts from "typescript";
import type { DiagnosticWriter } from "./diagnostics";
export declare class Project {
    private diagnosticWriter;
    private cmdLine;
    private projectFiles;
    private externalFiles;
    private languageService;
    private writeFile;
    constructor(documentRegistry: ts.DocumentRegistry, diagnosticWriter: DiagnosticWriter, cmdLine: ts.ParsedCommandLine, enhanceLanguageServiceHost?: (host: ts.LanguageServiceHost) => void);
    getCommandLine(): ts.ParsedCommandLine;
    private getFileDiagnostics;
    validateFile(fileName: string): boolean;
    validate(): boolean;
    emitFile(fileName: string): boolean;
    emit(): boolean;
    hasFile(fileName: string): boolean;
    setFile(fileName: string, snapshot?: ts.IScriptSnapshot): void;
    removeFile(fileName: string): void;
    removeAllFiles(): void;
    dispose(): void;
}
//# sourceMappingURL=project.d.ts.map