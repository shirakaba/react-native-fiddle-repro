import type { Terminal } from "metro-core";
import type { TerminalReporter } from "metro/src/lib/TerminalReporter";
export type MetroTerminal = {
    terminal: Terminal;
    reporter: TerminalReporter;
};
export declare function makeReporter(customReporterPath: string | undefined, terminal: Terminal, projectRoot: string): TerminalReporter;
export declare function makeTerminal(projectRoot: string): Terminal;
//# sourceMappingURL=terminal.d.ts.map