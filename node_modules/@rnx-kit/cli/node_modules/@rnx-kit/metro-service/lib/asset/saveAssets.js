"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSaveAssetsPlugin = getSaveAssetsPlugin;
const cli_1 = require("@rnx-kit/tools-react-native/cli");
const path = __importStar(require("node:path"));
const android_1 = require("./android");
const default_1 = require("./default");
const ios_1 = require("./ios");
// Eventually this will be part of the rn config, but we require it on older rn
// versions for win32 and the cli doesn't allow extra config properties.
// See https://github.com/react-native-community/cli/pull/2002
function getSaveAssetsPlugin(platform, projectRoot) {
    if (platform === "win32") {
        try {
            const saveAssetsPlugin = require.resolve("@office-iss/react-native-win32/saveAssetPlugin", { paths: [projectRoot] });
            return require(saveAssetsPlugin);
        }
        catch (_) {
            /* empty */
        }
    }
    // Use `@react-native/community-cli-plugin` when possible
    const pluginPath = (0, cli_1.findCommunityCliPluginPath)(projectRoot);
    if (pluginPath) {
        const { default: saveAssets } = require(path.join(pluginPath, "dist", "commands", "bundle", "saveAssets.js"));
        return saveAssets;
    }
    switch (platform) {
        case "ios":
            return ios_1.saveAssetsIOS;
        case "android":
            return android_1.saveAssetsAndroid;
        default:
            return default_1.saveAssetsDefault;
    }
}
//# sourceMappingURL=saveAssets.js.map