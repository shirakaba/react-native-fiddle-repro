"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveAssetsDefault = void 0;
exports.getAssetDestPath = getAssetDestPath;
const path_1 = __importDefault(require("path"));
function getAssetDestPath(asset, scale) {
    const suffix = scale === 1 ? "" : `@${scale}x`;
    const fileName = `${asset.name + suffix}.${asset.type}`;
    return path_1.default.join(
    // Assets can have relative paths outside of the project root.
    // Replace `../` with `_` to make sure they don't end up outside of
    // the expected assets directory.
    asset.httpServerLocation.substr(1).replaceAll("../", "_"), fileName);
}
const saveAssetsDefault = (assets, _platform, _assetsDest, _assetCatalogDest, addAssetToCopy) => {
    assets.forEach((asset) => addAssetToCopy(asset, undefined, getAssetDestPath));
};
exports.saveAssetsDefault = saveAssetsDefault;
//# sourceMappingURL=default.js.map