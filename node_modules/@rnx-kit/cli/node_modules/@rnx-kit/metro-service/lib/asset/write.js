"use strict";
// https://github.com/react-native-community/cli/blob/716555851b442a83a1bf5e0db27b6226318c9a69/packages/cli-plugin-metro/src/commands/bundle/saveAssets.ts
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.saveAssets = saveAssets;
const console_1 = require("@rnx-kit/console");
const fs = __importStar(require("node:fs"));
const path = __importStar(require("node:path"));
const filter_1 = require("./filter");
function copy(src, dest, callback) {
    const destDir = path.dirname(dest);
    fs.mkdir(destDir, { recursive: true }, (err) => {
        if (err) {
            callback(err);
            return;
        }
        fs.createReadStream(src)
            .pipe(fs.createWriteStream(dest))
            .on("finish", callback);
    });
}
function copyAll(filesToCopy) {
    const queue = Object.keys(filesToCopy);
    if (queue.length === 0) {
        return Promise.resolve();
    }
    (0, console_1.info)(`Copying ${queue.length} asset files`);
    return new Promise((resolve, reject) => {
        const copyNext = (error) => {
            if (error) {
                reject(error);
                return;
            }
            if (queue.length === 0) {
                (0, console_1.info)("Done copying assets");
                resolve();
            }
            else {
                // queue.length === 0 is checked in previous branch, so this is string
                const src = queue.shift();
                const dest = filesToCopy[src];
                copy(src, dest, copyNext);
            }
        };
        copyNext();
    });
}
function saveAssets(assets, platform, assetsDest, assetCatalogDest, saveAssetsPlugin) {
    if (!assetsDest) {
        (0, console_1.warn)("Assets destination folder is not set, skipping...");
        return Promise.resolve();
    }
    const filesToCopy = Object.create(null); // Map src -> dest
    const addAssetToCopy = (asset, allowedScales, getAssetDestPath) => {
        const validScales = new Set((0, filter_1.filterPlatformAssetScales)(allowedScales, asset.scales));
        asset.scales.forEach((scale, idx) => {
            if (!validScales.has(scale)) {
                return;
            }
            const src = asset.files[idx];
            const dest = path.join(assetsDest, getAssetDestPath(asset, scale));
            filesToCopy[src] = dest;
        });
        asset.scales.forEach((scale, idx) => {
            if (!validScales.has(scale)) {
                return;
            }
            const src = asset.files[idx];
            const dest = path.join(assetsDest, getAssetDestPath(asset, scale));
            filesToCopy[src] = dest;
        });
    };
    saveAssetsPlugin(assets, platform, assetsDest, assetCatalogDest, addAssetToCopy);
    return copyAll(filesToCopy);
}
//# sourceMappingURL=write.js.map