"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildBundle = buildBundle;
const console_1 = require("@rnx-kit/console");
const metro_1 = require("@rnx-kit/tools-react-native/metro");
const saveAssets_1 = require("../asset/saveAssets");
const write_1 = require("../asset/write");
/**
 * Adds the `assets` property to the `BuildOutput` type.
 * @see {@link https://github.com/facebook/metro/pull/1511}
 */
function withAssets(bundle) {
    return bundle;
}
function getAssets(metroServer, projectRoot, requestOptions) {
    const MetroServer = (0, metro_1.requireModuleFromMetro)("metro/src/Server", projectRoot);
    return metroServer.getAssets({
        ...MetroServer.DEFAULT_BUNDLE_OPTIONS,
        ...requestOptions,
    });
}
// Source: https://github.com/facebook/metro/blob/v0.82.4/packages/metro/src/index.flow.js#L386
async function buildBundle(args, config, output, requestOptions) {
    const { runMetro } = (0, metro_1.requireModuleFromMetro)("metro", config.projectRoot);
    const metroServer = await runMetro(config, { watch: false });
    try {
        const metroBundle = await output.build(metroServer, requestOptions, {
            withAssets: Boolean(args.assetsDest),
        });
        const bundleOutput = args.bundleOutput;
        if (bundleOutput) {
            const { dev, platform, sourcemapOutput } = args;
            const outputOptions = { bundleOutput, sourcemapOutput, dev, platform };
            await output.save(metroBundle, outputOptions, (message) => config.reporter.update({
                type: "bundle_save_log",
                message,
            }));
        }
        if (!args.assetsDest) {
            (0, console_1.warn)("Assets destination folder is not set, skipping...");
            return;
        }
        const assets = withAssets(metroBundle).assets ??
            (await getAssets(metroServer, config.projectRoot, requestOptions));
        // When we're done saving bundle output and the assets, we're done.
        await (0, write_1.saveAssets)(assets, args.platform, args.assetsDest, args.assetCatalogDest, (0, saveAssets_1.getSaveAssetsPlugin)(args.platform, config.projectRoot));
    }
    finally {
        metroServer.end();
    }
}
//# sourceMappingURL=bundle-0.71.js.map